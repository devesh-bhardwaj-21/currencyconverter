ktor {
    deployment {
        port = 8080
        port = ${?PORT}
        watch = [ com.currencyconverter.ApplicationKt.module ]
    }
    application {
        modules = [ com.currencyconverter.ApplicationKt.module ]
    }
}
